# okanga-mcp
custom MCP server script that optimizes AI coding tools such as Claude Code and Codex by minimizing token usage when parsing code and log files.  Optimized for Swift language for now, but fully extensible.
